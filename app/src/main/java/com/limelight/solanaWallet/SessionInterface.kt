package com.limelight.solanaWallet

class SessionInterface {
}

/*
fun simulateManualImmission(ipAddress: String?, publicKey: String) {
    hostText.setText(ipAddress)
    // Store the public key somewhere, possibly in an instance variable
    this.storedPublicKey = publicKey
    handleDoneEventShaga()
}
 */