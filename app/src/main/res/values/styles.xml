<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.

    -->
    <style name="AppBaseTheme" parent="android:Theme">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.

        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/iris_canvas</item>
        <item name="android:colorAccent">@color/iris_indigo</item>
        <item name="android:fontFamily">sans</item>
        <item name="android:textColorPrimary">@color/iris_text_primary</item>
        <item name="android:textColorSecondary">@color/iris_text_secondary</item>
        <item name="android:windowAnimationStyle">@style/IrisWindowAnimation</item>
        <item name="android:alertDialogTheme">@style/IrisAlertDialog</item>
    </style>

    <style name="IrisAlertDialog" parent="android:Theme.Material.Dialog.Alert">
        <item name="android:windowBackground">@drawable/iris_glass_dark_panel</item>
        <item name="android:colorAccent">@color/iris_cyan</item>
        <item name="android:textColorPrimary">@color/iris_text_primary</item>
        <item name="android:textColorSecondary">@color/iris_text_secondary</item>
        <item name="android:backgroundDimAmount">0.72</item>
    </style>

    <style name="IrisWindowAnimation">
        <item name="android:activityOpenEnterAnimation">@anim/screen_fade_in</item>
        <item name="android:activityOpenExitAnimation">@anim/screen_fade_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/screen_fade_in</item>
        <item name="android:activityCloseExitAnimation">@anim/screen_fade_out</item>
    </style>

    <style name="IrisHeaderText">
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textColor">@color/iris_text_primary</item>
        <item name="android:textSize">28sp</item>
    </style>

    <style name="IrisHeroTitle">
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textColor">@color/iris_text_primary</item>
        <item name="android:textSize">42sp</item>
    </style>

    <style name="IrisEyebrowText">
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textColor">@color/iris_cyan</item>
        <item name="android:textSize">13sp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:letterSpacing">0.12</item>
    </style>

    <style name="StreamBaseTheme" parent="AppBaseTheme">
        <!-- Transparent streaming background to avoid extra overdraw -->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- Stream activity theme -->
    <style name="StreamTheme" parent="StreamBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
